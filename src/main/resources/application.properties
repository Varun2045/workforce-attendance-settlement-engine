# Server Port
server.port=8080

# --- SUPABASE POSTGRESQL CONFIGURATION ---
# IMPORTANT: Using port 6543 for Session pooling (Ticket LF-205)
spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.username=postgres.oofcvthwtuoyeryjdfqy
spring.datasource.password=DXgksINBOZNiWsN7
spring.datasource.driver-class-name=org.postgresql.Driver

# --- TICKET LF-205: CONNECTION POOL TUNING ---
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.max-lifetime=240000
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.connection-timeout=15000
spring.datasource.hikari.idle-timeout=120000

# --- HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# --- REDIS CONFIGURATION ---
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.timeout=2000ms
spring.data.redis.connect-timeout=2000ms

# --- APPLICATION CUSTOM CONFIGURATION ---
# Configurable allowed origins for CORS (TICKET LF-201)
app.security.allowed-origins=http://localhost:3000,http://localhost:5173,https://employee-management-system-frontend-hazel.vercel.app/

# External Government Minimum Wage Rate API (TICKET LF-205)
app.external.wage-api-url=https://api.gov.in/wage-rates
app.external.wage-api.connect-timeout=2000
app.external.wage-api.read-timeout=3000
